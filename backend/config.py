# The database path
DB_PATH = 'pokemon.db'

# Web server port
PORT = 8080

# Web server host, it typically be localhost or 127.0.0.1,
# If deploy web application on a cloud server, it should bind to 0.0.0.0
HOST = 'localhost'